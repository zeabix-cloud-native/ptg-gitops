apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admin-portal.fullname" . }}-test-connection"
  labels:
    {{- include "admin-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admin-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
