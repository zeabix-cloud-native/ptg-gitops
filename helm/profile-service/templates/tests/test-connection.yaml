apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "profile-service.fullname" . }}-test-connection"
  labels:
    {{- include "profile-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "profile-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
